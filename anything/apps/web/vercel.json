{
    "buildCommand": "npm run build",
    "outputDirectory": "build/client",
    "functions": {
        "api/index.mjs": {
            "includeFiles": "{build/server/**,package.json}",
            "maxDuration": 60,
            "regions": [
                "iad1"
            ]
        }
    },
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api"
        }
    ]
}
